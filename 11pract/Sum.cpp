#include "Header.h"

using namespace std;
int sum(int fst, int sec)
{
	return (fst + sec);
}
double sum(double fst, double sec)
{
	return (fst + sec);
}
string sum(string fst, string sec)
{
	return (fst + sec);
}